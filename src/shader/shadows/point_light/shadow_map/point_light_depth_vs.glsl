#include <../../../common/mesh_vertex_attribs.glsl>
#include <../../../common/uniforms.glsl>

// ------------------------------------------------------------------
// OUTPUT  ----------------------------------------------------------
// ------------------------------------------------------------------

out vec3 PS_IN_FragPos;

// ------------------------------------------------------------------
// MAIN  ------------------------------------------------------------
// ------------------------------------------------------------------

void main()
{
	vec4 frag_pos = model_mat * vec4(VS_IN_Position, 1.0f);
	PS_IN_FragPos = frag_pos.xyz;
	gl_Position = view_proj * frag_pos;
}

// ------------------------------------------------------------------