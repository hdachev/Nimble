#include <../../../common/uniforms.glsl>

// ------------------------------------------------------------------
// INPUT VARIABLES  -------------------------------------------------
// ------------------------------------------------------------------

#ifdef BLEND_MODE_MASKED
in vec2 FS_IN_TexCoord;
#endif

// ------------------------------------------------------------------
// MAIN -------------------------------------------------------------
// ------------------------------------------------------------------

void main()
{
#ifdef BLEND_MODE_MASKED
    float alpha = texture(s_Albedo, FS_IN_TexCoord).w;
	// Discard fragments below alpha threshold
	if (alpha < 0.1)
		discard;
#endif
}

// ------------------------------------------------------------------